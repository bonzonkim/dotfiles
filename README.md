## ColorScheme
    * Molokai 

## Configured LSP
    * pyright 
    * tailwindcss 
    * terraformls 
    * gopls 
    * tsserver 

## Plugin Manager
    * Packer 


** You can find the all key maps in lua/maps.lua **
---
# Karabiner key mappings
`<C-[>` to `<ESC>`  
`<Shift-space>` to F19 (I personally map this to change input source)  
`<right cmd-i,j,k,l>` to arrow keys  
`<ESC>` to `<ESC>`  and automatically change input source to ABC

